-ตั้งใจทำเพื่อเธอเลยนะอันนี้ 
-หวังว่าจะได้ใช้จนครบนะ


## Admin / Data export
- เพิ่มหน้า `admin.php` สำหรับดูรายการการใส่รหัส (PIN) และดาวน์โหลด CSV
- เปลี่ยนรหัสแอดมินที่ `api/config.php` (ค่าตั้งต้น: `admin123`) — **เปลี่ยนก่อนนำขึ้นโฮสต์**
- ฐานข้อมูล SQLite เก็บที่ `data/pins.sqlite` (ไฟล์จะถูกสร้างโดย `api/submit_pin.php`)

⚠️ Tip: ให้ตั้งสิทธิ์โฟลเดอร์ `data/` เป็นที่เว็บเซิร์ฟเวอร์เขียนได้ เมื่อ deploy บนโฮสต์จริง